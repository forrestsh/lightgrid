# LIG-16 · 手机端第一帧兼容性

## 用户故事

作为已完成模型初始化的手机玩家，我希望第一帧动画能在旧版 Safari/WebView 中运行，以便进入已经生成的 3D 世界。

## 验收标准

1. 每帧地标标签更新不依赖 `HTMLCollection` 迭代器。
2. 不使用展开参数读取地标坐标，改为显式坐标访问。
3. 第一帧错误区分为 `frame: tag projection` 与 `frame: GPU render`。
4. 桌面及手机布局的地标投影、画布和交互保持不变。
