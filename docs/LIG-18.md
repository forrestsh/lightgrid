# LIG-18 · 手机端基础着色器路径

## 用户故事

作为在 GPU render 阶段仍失败的手机玩家，我希望断桥谷使用最基础的单着色器 3D 路径，以便避开移动 GPU 的 PBR、光照和透明材质编译差异。

## 验收标准

1. 手机所有世界实例和化身使用 `MeshBasicMaterial`。
2. 手机 WebGL 着色精度使用 `lowp`。
3. 手机不编译光照、PBR、透明或发光材质分支。
4. 颜色、FCC 轮廓、深度、相机和交互仍保留。
5. 桌面 PBR 路径完全不变。
