# LIG-19 · 手机版 3D 点云渲染

## 用户故事

作为在所有实例材质路径下仍于 GPU render 阶段失败的手机玩家，我希望断桥谷绕过实例化网格提交，同时保留可旋转、可缩放和有空间深度的 3D 世界。

## 验收标准

1. 手机世界单元按语义类型使用 `THREE.Points` 绘制，不创建世界 `InstancedMesh`。
2. 地标颜色、相机、雾、移动、任务、选择和自主阶段继续工作。
3. 手机化身仍为小型 FCC 3D 网格，保持身体可读性。
4. 桌面继续使用完整菱形十二面体 `InstancedMesh`。
5. 手机世界点云通过首帧和持续渲染验证。
